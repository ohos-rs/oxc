# GitHub Actions policy

This long-lived OHOS branch intentionally does not contain GitHub Actions workflow definitions.

When synchronizing changes from upstream, do not restore `*.yml` or `*.yaml` files in this directory unless GitHub Actions is explicitly re-enabled for this repository.
